"use client";
import {FormEvent,useState} from "react"; import {useRouter} from "next/navigation";
export default function Setup(){const r=useRouter();const [f,setF]=useState({name:"",email:"",password:""});const [msg,setMsg]=useState("");async function submit(e:FormEvent){e.preventDefault();const res=await fetch("/api/auth/setup",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(f)});const d=await res.json();if(!res.ok){setMsg(d.message||"Setup failed");return;}r.replace("/dashboard");r.refresh();}return <main className="min-h-screen bg-slate-100 flex items-center justify-center p-4"><form onSubmit={submit} className="w-full max-w-md rounded-2xl bg-white p-8 shadow"><h1 className="text-2xl font-bold">Initial Setup</h1><p className="text-sm text-slate-500">Create the first Super Admin. This works only when no users exist.</p>{msg&&<p className="mt-4 text-sm text-red-600">{msg}</p>}{(["name","email","password"] as const).map(k=><input key={k} className="mt-4 w-full rounded-lg border p-3" type={k==="password"?"password":k==="email"?"email":"text"} placeholder={k[0].toUpperCase()+k.slice(1)} value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})} required/>)}<button className="mt-6 w-full rounded-lg bg-blue-600 p-3 font-semibold text-white">Create Super Admin</button></form></main>}
