import { NextResponse } from "next/server";
import { db } from "@/src/prisma/db";

function clean(body:any){return {title:String(body.title||"").trim(),category:String(body.category||"Other"),amount:Number(body.amount),paymentMethod:String(body.paymentMethod||"Cash"),date:new Date(`${String(body.date).slice(0,10)}T00:00:00`),description:String(body.description||"").trim(),status:String(body.status||"Paid")}}
export async function GET(){try{const expenses=await db.orm.public.Expense.all();return NextResponse.json({expenses:expenses.sort((a:any,b:any)=>b.id-a.id)})}catch(e){console.error("GET EXPENSES:",e);return NextResponse.json({error:"Failed to load expenses."},{status:500})}}
export async function POST(req:Request){try{const b=clean(await req.json());if(!b.title||!Number.isFinite(b.amount)||b.amount<=0)return NextResponse.json({error:"Valid title and amount are required."},{status:400});const created=await db.transaction(async(tx)=>{const row=await tx.orm.public.Expense.create({expenseNo:"PENDING",...b});return await tx.orm.public.Expense.where({id:row.id}).update({expenseNo:`EXP-${String(row.id).padStart(4,"0")}`})});return NextResponse.json({expense:created},{status:201})}catch(e){console.error("POST EXPENSE:",e);return NextResponse.json({error:"Failed to save expense."},{status:500})}}
