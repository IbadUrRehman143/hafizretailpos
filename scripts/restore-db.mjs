import {spawnSync} from "node:child_process"; const file=process.argv[2]; if(!file||!process.env.DATABASE_URL){console.error("Usage: npm run db:restore -- backups/file.sql (DATABASE_URL required)");process.exit(1)} const r=spawnSync("psql",[process.env.DATABASE_URL,"-f",file],{stdio:"inherit"});process.exit(r.status??1);
